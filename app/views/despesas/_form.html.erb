<div class="container">
<%= form_for(@despesa, remote: true) do |f| %>
  <% if @despesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@despesa.errors.count, "error") %> prohibited this despesa from being saved:</h2>

      <ul>
      <% @despesa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group center">
    <%= f.label :data, class: "control-label" %><br>
    <%= f.date_select :data, class: "form-control" %>
  </div>
  <div class="form-group center">
    <%= f.label :time, class: "control-label" %><br>
    <%= f.time_select :time, class: "form-control" %>
  </div>
  <div class="form-group center">
    <%= f.label :description, class: "control-label" %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group center">
    <%= f.label :amount, class: "control-label" %><br>
    <%= f.number_field :amount, class: "form-control" %>
  </div>
  <div class="form-group center">
    <%= f.label :comment, class: "control-label" %><br>
    <%= f.text_field :comment, class: "form-control" %>
  </div>
  <div class="form-group center">
    <%= f.submit %>
  </div>
<% end %>
</div>